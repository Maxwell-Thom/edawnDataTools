export default function Lead(firstName, lastName, uuid, domain, email, status, source) {
  this.firstName = firstName
  this.lastName = lastName
  this.uuid = uuid
  this.domain = domain
  this.email = email
  this.status = status
  this.source = source
}
